<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lab1.Mappers.StudentMapper">

    <select id="optionalSelect" resultType="lab1.dto.Student">
        SELECT ${select} FROM student WHERE ${where}
    </select>
</mapper>